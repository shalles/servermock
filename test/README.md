##servermock test demo

**first**

```
npm isntall -g servermock
```

**second**
set server and mock url and mock data path and another config in sm.config

set mock random data variable in .mockrc

**then**

```
cd test

servermock start
```
